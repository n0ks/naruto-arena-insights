import React from "react";
import { View } from "react-native";
import { NavigationType, ICharacters } from "../../utils";
import styles from "./{{pascalCase name}}.styles";

interface Props {

}

export const {{pascalCase name}}: React.SFC<NavigationType> = ({ navigation }) => {
  return (
    <View>{{pascalCase name}}</View>
  )
};